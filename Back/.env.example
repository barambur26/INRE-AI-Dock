# AI Dock App - Environment Configuration
# Copy this file to .env and customize for your environment

# =============================================================================
# LLM API KEYS & CONFIGURATION
# =============================================================================

# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_ORGANIZATION=your-org-id-here  # Optional

# Anthropic Claude API Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Azure OpenAI Configuration
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_VERSION=2023-05-15

# Local Ollama Configuration
# OLLAMA_BASE_URL=http://localhost:11434  # Default Ollama URL

# =============================================================================
# JWT AUTHENTICATION SETTINGS
# =============================================================================

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate with: python -c "from app.core.security import generate_jwt_secret; print(generate_jwt_secret())"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production-use-generate-jwt-secret-function

# JWT Algorithm (recommended: HS256)
JWT_ALGORITHM=HS256

# Access token expiration (in minutes)
# Short-lived for security (15 minutes is recommended)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15

# Refresh token expiration (in days)
# Regular refresh tokens (7 days is recommended)
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Remember me refresh tokens (30 days max recommended)
JWT_REFRESH_TOKEN_REMEMBER_ME_EXPIRE_DAYS=30

# =============================================================================
# PASSWORD SECURITY SETTINGS
# =============================================================================

# BCrypt rounds (higher = more secure but slower)
# 12 is a good balance between security and performance
BCRYPT_ROUNDS=12

# =============================================================================
# DATABASE SETTINGS
# =============================================================================

# PostgreSQL (Production)
DATABASE_URL=postgresql://aidock:aidock@localhost:5432/aidock
DATABASE_URL_ASYNC=postgresql+asyncpg://aidock:aidock@localhost:5432/aidock

# SQLite (Development/Testing)
# DATABASE_URL=sqlite:///./aidock.db
# DATABASE_URL_ASYNC=sqlite+aiosqlite:///./aidock.db

# =============================================================================
# API SETTINGS
# =============================================================================

# API version prefix
API_V1_STR=/api/v1

# Project information
PROJECT_NAME=AI Dock App
PROJECT_VERSION=1.0.0

# Environment
ENVIRONMENT=development
DEBUG=true

# =============================================================================
# CORS SETTINGS
# =============================================================================

# Frontend URLs (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173

# =============================================================================
# RATE LIMITING
# =============================================================================

# Login rate limiting (format: "requests/timeframe")
RATE_LIMIT_LOGIN=5/minute

# Refresh token rate limiting
RATE_LIMIT_REFRESH=10/minute

# =============================================================================
# REDIS SETTINGS (for caching and rate limiting)
# =============================================================================

# Redis URL for caching and session storage
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# SECURITY HEADERS
# =============================================================================

# Enable security headers in production
ENABLE_SECURITY_HEADERS=true

# =============================================================================
# LOGGING
# =============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Log format (json or text)
LOG_FORMAT=json

# =============================================================================
# TESTING SETTINGS
# =============================================================================

# Test database (separate from production)
TEST_DATABASE_URL=sqlite:///./test_aidock.db

# Test JWT secret (use different from production)
TEST_JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only

# =============================================================================
# PRODUCTION SECURITY NOTES
# =============================================================================

# 🔐 IMPORTANT SECURITY REMINDERS:
# 
# 1. ALWAYS change JWT_SECRET_KEY in production
#    - Use: python -c "from app.core.security import generate_jwt_secret; print(generate_jwt_secret())"
#    - Keep it secret and never commit to version control
#
# 2. Use strong database passwords in production
#    - Never use default passwords like 'aidock'
#
# 3. Set DEBUG=false in production
#
# 4. Use HTTPS in production
#    - Set secure cookie settings
#    - Enable HSTS headers
#
# 5. Configure proper CORS origins
#    - Only allow trusted domains
#
# 6. Set up proper logging and monitoring
#
# 7. Regularly rotate JWT secrets
#
# 8. Use Redis for token blacklisting in production
#    - The in-memory blacklist is only for development

# =============================================================================
# EXAMPLE PRODUCTION VALUES
# =============================================================================

# Example production settings (customize for your environment):
#
# JWT_SECRET_KEY=RkFzdEFQSUlzQXdlc29tZS1TdXBlclNlY3VyZUtleUZvclByb2R1Y3Rpb24tQ2hhbmdlVGhpcw
# DATABASE_URL=postgresql://aidock_prod:super_secure_password@db.example.com:5432/aidock_prod
# ENVIRONMENT=production
# DEBUG=false
# BACKEND_CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# REDIS_URL=redis://redis.example.com:6379/0
